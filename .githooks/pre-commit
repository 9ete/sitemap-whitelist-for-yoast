#!/bin/sh

# ──────────────────────────────────────────────────────────────────────
# pre-commit hook
#
# Auto-fix formatting so staged files are clean before the commit-message
# editor opens. Lint + tests run in the commit-msg hook (after the
# conventional-commit message is validated, before the commit is finalised).
# ──────────────────────────────────────────────────────────────────────

composer lint:fix
